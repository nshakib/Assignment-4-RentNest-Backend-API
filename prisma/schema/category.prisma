model Category {
    id        String       @id @default(uuid())
    name       String   @unique @db.VarChar(100)

    createdAt  DateTime @default(now())
    updatedAt  DateTime @updatedAt

    // one to many Relations
    properties Property[] //A category can have many properties

    @@map("category")
}